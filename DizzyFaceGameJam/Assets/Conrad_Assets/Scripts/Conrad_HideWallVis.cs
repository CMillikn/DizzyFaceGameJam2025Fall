using UnityEngine;

public class Conrad_HideWallVis : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public SpriteRenderer redSR;
    void Start()
    {
        redSR.enabled = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
